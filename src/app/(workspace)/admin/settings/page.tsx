import { requirePage } from "@/shared/auth/account";
import { companySettings } from "@/modules/settings/infrastructure/repository";
import { PageHeading } from "@/components/page-heading";
import { CommandForm } from "@/components/command-form";
import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";
import Link from "next/link";
export default async function SettingsPage(){const account=await requirePage("RBAC_MANAGE");const settings=await companySettings();const allocation=settings.find(row=>row.key==="project.allocation");const mode=allocation?.value.mode==="BLOCK"?"BLOCK":"WARN";return <><PageHeading eyebrow="SYSTEM POLICY" title="시스템 설정" description="업무 규칙의 강제 수준을 코드 변경 없이 전환합니다. 모든 변경은 감사 로그에 기록됩니다."/><div className="space-y-5"><Card className="max-w-2xl"><CardHeader><CardTitle>프로젝트 투입률 정책</CardTitle></CardHeader><CardContent><p className="mb-5 text-xs leading-6 text-muted-foreground">경고 모드는 100% 초과 투입을 저장하면서 사용자에게 알리고, 차단 모드는 트랜잭션을 거부합니다.</p>{allocation?<CommandForm endpoint="/api/system-settings" action="setting.save" constants={{key:allocation.key,version:allocation.version}} fields={[{name:"mode",label:"초과 투입 처리",type:"select",value:mode,required:true,options:[{value:"WARN",label:"경고 후 저장"},{value:"BLOCK",label:"저장 차단"}]}]} submit="정책 저장"/>:null}</CardContent></Card>{account.permissions.includes("CARD_SETTLEMENT_READ")&&<Card className="max-w-2xl"><CardHeader><CardTitle>법인카드 정산</CardTitle></CardHeader><CardContent><p className="mb-4 text-xs leading-6 text-muted-foreground">자동 발송, 수신자, 발송시간과 정산 파일을 관리합니다.</p><Link href="/admin/card-settlements" className="text-sm font-semibold text-primary hover:underline">정산 설정 및 이력 열기 →</Link></CardContent></Card>}</div></>}
