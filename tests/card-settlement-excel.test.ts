import {describe,expect,it} from "vitest";
import ExcelJS from "exceljs";
import {buildSettlementExcel} from "../src/modules/corporate-card/application/settlement-excel";
import type {z} from "zod";
import type {settlementSchema} from "../src/modules/corporate-card/infrastructure/repository";
describe("monthly card Excel",()=>{it("writes actual snapshot values, totals and safe text cells",async()=>{const report={id:"10000000-0000-4000-8000-000000000001",settlement_month:"2026-09-01",status:"CLOSED",total_count:1,total_supply_amount:10000,total_vat_amount:1000,total_amount:11000,entries_snapshot:[{id:"10000000-0000-4000-8000-000000000002",transaction_date:"2026-09-12",project_code:"DS-01",project_name:"Project",card:"Company •••• 1234",user_name:"Employee",merchant:"=SUM(A1:A2)",purpose:"Meeting",category:"MEETING",supply_amount:10000,vat_amount:1000,total_amount:11000,memo:"Memo"}],file_key:null,checksum:null,generated_at:"2026-10-01T00:00:00Z",generated_by:null,sent_at:null,send_generation:0,version:1,created_at:"2026-10-01T00:00:00Z",updated_at:"2026-10-01T00:00:00Z"} satisfies z.infer<typeof settlementSchema>;const content=await buildSettlementExcel(report);const workbook=new ExcelJS.Workbook();await workbook.xlsx.load(content as unknown as Parameters<typeof workbook.xlsx.load>[0]);const sheet=workbook.getWorksheet("법인카드 사용내역")!;expect(sheet.getCell("A5").value).toBe("2026-09-12");expect(sheet.getCell("F5").value).toBe("'=SUM(A1:A2)");expect(sheet.getCell("I5").value).toBe(10000);expect(sheet.getCell("J5").value).toBe(1000);expect(sheet.getCell("K5").value).toBe(11000);expect(sheet.getCell("K6").value).toBe(11000)})});
